defaults:
    run:
        shell: bash
jobs:
    comment-argocd-patch-commands:
        if: |
            github.event.action != 'closed' && github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal'
        needs:
            -   release-helm-charts
        permissions:
            pull-requests: write
        runs-on: ubuntu-latest
        steps:
            -   env:
                    GH_TOKEN: ${{github.token}}
                    PR_NUMBER: ${{github.event.pull_request.number}}
                    REGISTRY: ghcr.io/${{github.repository_owner}}/charts-pr/${{github.event.pull_request.number}}
                    SHA: ${{github.sha}}
                name: Post ArgoCD patch commands
                run: |
                    set -o errexit
                    set -o nounset
                    set -o pipefail

                    SHORT_SHA="${SHA:0:7}"

                    _chart_version() {
                        gh api repos/${{github.repository}}/contents/cloud/helm/"${1}"/Chart.yaml?ref="${SHA}" --jq '.content' \
                            | base64 --decode \
                            | yq .version
                    }

                    GCP_VERSION="$(_chart_version gcp)-pr-${PR_NUMBER}-g${SHORT_SHA}"
                    GCP_INFRA_VERSION="$(_chart_version gcp-infrastructure)-pr-${PR_NUMBER}-g${SHORT_SHA}"
                    GCP_INFRA_PROVIDER_VERSION="$(_chart_version gcp-infrastructure-provider)-pr-${PR_NUMBER}-g${SHORT_SHA}"

                    cat > /tmp/pr-comment.md << EOF
                    ## GCP Helm Charts Published

                    Charts for this PR are available at \`${REGISTRY}\`. To test on a cluster, set your ArgoCD namespace and run:

                    \`\`\`bash
                    ARGOCD_NS=\$(kubectl get pods -A -l app.kubernetes.io/name=argocd-server -o jsonpath='{.items[0].metadata.namespace}')

                    # Permit GHCR in ArgoCD AppProjects (only needed once per cluster)
                    kubectl patch appproject liferay-application -n "\${ARGOCD_NS}" --type=json -p='[{"op":"add","path":"/spec/sourceRepos/-","value":"${REGISTRY}"}]'
                    kubectl patch appproject liferay-infrastructure -n "\${ARGOCD_NS}" --type=json -p='[{"op":"add","path":"/spec/sourceRepos/-","value":"${REGISTRY}"}]'

                    # Update ApplicationSets and Application to use PR charts
                    kubectl patch applicationset liferay-applicationset -n "\${ARGOCD_NS}" --type=json -p='[
                      {"op":"replace","path":"/spec/template/spec/sources/0/repoURL","value":"${REGISTRY}"},
                      {"op":"replace","path":"/spec/template/spec/sources/0/targetRevision","value":"${GCP_VERSION}"},
                      {"op":"replace","path":"/spec/template/spec/sources/0/chart","value":"liferay-gcp"}
                    ]'
                    kubectl patch applicationset liferay-infrastructure-applicationset -n "\${ARGOCD_NS}" --type=json -p='[
                      {"op":"replace","path":"/spec/template/spec/sources/0/repoURL","value":"${REGISTRY}"},
                      {"op":"replace","path":"/spec/template/spec/sources/0/targetRevision","value":"${GCP_INFRA_VERSION}"},
                      {"op":"replace","path":"/spec/template/spec/sources/0/chart","value":"liferay-gcp-infrastructure"}
                    ]'
                    kubectl patch application liferay-infrastructure-provider -n "\${ARGOCD_NS}" --type=json -p='[
                      {"op":"replace","path":"/spec/sources/0/repoURL","value":"${REGISTRY}"},
                      {"op":"replace","path":"/spec/sources/0/targetRevision","value":"${GCP_INFRA_PROVIDER_VERSION}"},
                      {"op":"replace","path":"/spec/sources/0/chart","value":"liferay-gcp-infrastructure-provider"}
                    ]'
                    \`\`\`
                    EOF

                    gh pr comment "${PR_NUMBER}" --edit-last --body-file /tmp/pr-comment.md 2>/dev/null || \
                        gh pr comment "${PR_NUMBER}" --body-file /tmp/pr-comment.md
    delete-packages-pr:
        if: github.event.action == 'closed' && github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal'
        runs-on: ubuntu-latest
        steps:
            -   env:
                    GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
                    ORG: ${{github.repository_owner}}
                    PACKAGE: charts-pr/${{github.event.pull_request.number}}/${{matrix.chart}}
                name: Delete ephemeral PR package from GHCR
                run: |
                    set -o errexit
                    set -o nounset
                    set -o pipefail

                    PACKAGE_ENC=$(jq --arg p "${PACKAGE}" --null-input --raw-output '$p|@uri')

                    if gh api "/orgs/${ORG}/packages/container/${PACKAGE_ENC}" > /dev/null 2>&1
                    then
                        echo "Deleting package ${PACKAGE} from ${ORG}."

                        gh api \
                            --method DELETE \
                            "/orgs/${ORG}/packages/container/${PACKAGE_ENC}"
                    else
                        echo "No package ${PACKAGE} was found under ${ORG}."
                    fi
        strategy:
            matrix:
                chart:
                    -   liferay-aws
                    -   liferay-aws-infrastructure
                    -   liferay-aws-infrastructure-provider
                    -   liferay-default
                    -   liferay-gcp
                    -   liferay-gcp-infrastructure
                    -   liferay-gcp-infrastructure-provider
    delete-releases-pr:
        env:
            PR_NUMBER: ${{github.event.pull_request.number}}
        if: github.event.action == 'closed' && github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal'
        permissions:
            contents: write
        runs-on: ubuntu-latest
        steps:
            -   env:
                    GH_TOKEN: ${{github.token}}
                name: Delete preview GitHub Releases for closed PR
                run: |
                    set -o errexit
                    set -o nounset
                    set -o pipefail

                    PATTERN="0\\.0\\.0-pr-${PR_NUMBER}-"

                    local tags

                    tags=$(gh release list --limit 1000 --json tagName \ --jq ".[] | select(.tagName | test(\"${PATTERN}\")) | .tagName")

                    echo "${tags}" | while read -r tag
                    do
                        [ -z "${tag}" ] && continue

                        echo "Deleting release ${tag}."

                        gh release delete --cleanup-tag --yes "${tag}"
                    done
            -   name: Checkout gh-pages
                uses: actions/checkout@v4
                with:
                    fetch-depth: 1
                    ref: gh-pages
            -   name: Prune preview entries from index.yaml
                run: |
                    set -o errexit
                    set -o nounset
                    set -o pipefail

                    if [ ! -f index.yaml ]
                    then
                        echo "Nothing to prune on gh-pages."

                        exit 0
                    fi

                    PATTERN="^0\\.0\\.0-pr-${PR_NUMBER}-"

                    yq --inplace \
                        ".entries |= with_entries(.value |= map(select(.version | test(\"${PATTERN}\") | not)))" \
                        index.yaml

                    git config user.email "github-actions[bot]@users.noreply.github.com"
                    git config user.name "github-actions[bot]"

                    if ! git diff --quiet index.yaml
                    then
                        git add index.yaml

                        git commit --message "Remove preview entries for closed PR #${PR_NUMBER}."

                        git push
                    else
                        echo "No preview entries were found for PR #${PR_NUMBER}."
                    fi
    publish-aws:
        if: |
            (github.event_name == 'push' && github.repository == 'liferay/liferay-portal') ||
            (github.event.action != 'closed' && github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal')
        needs: publish-default
        secrets: inherit
        uses: ./.github/workflows/ci-reusable-publish-helm-chart-to-gar.yaml
        with:
            chart_name: liferay-aws
            pr_number: ${{github.event.pull_request.number}}
            registry: us-central1-docker.pkg.dev/liferay-artifact-registry/liferay-helm-chart
            working_directory: ./cloud/helm/aws
    publish-aws-infrastructure:
        if: |
            (github.event_name == 'push' && github.repository == 'liferay/liferay-portal') ||
            (github.event.action != 'closed' && github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal')
        secrets: inherit
        uses: ./.github/workflows/ci-reusable-publish-helm-chart-to-gar.yaml
        with:
            chart_name: liferay-aws-infrastructure
            pr_number: ${{github.event.pull_request.number}}
            registry: us-central1-docker.pkg.dev/external-assets-prd/liferay-helm-chart
            working_directory: ./cloud/helm/aws-infrastructure
    publish-aws-infrastructure-provider:
        if: |
            (github.event_name == 'push' && github.repository == 'liferay/liferay-portal') ||
            (github.event.action != 'closed' && github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal')
        secrets: inherit
        uses: ./.github/workflows/ci-reusable-publish-helm-chart-to-gar.yaml
        with:
            chart_name: liferay-aws-infrastructure-provider
            pr_number: ${{github.event.pull_request.number}}
            registry: us-central1-docker.pkg.dev/external-assets-prd/liferay-helm-chart
            working_directory: ./cloud/helm/aws-infrastructure-provider
    publish-aws-marketplace:
        env:
            ECR_REGISTRY: 709825985650.dkr.ecr.us-east-1.amazonaws.com
        if: github.event_name == 'push' && github.repository == 'liferay/liferay-portal'
        needs: publish-aws
        runs-on: ubuntu-latest
        steps:
            -   name: Configure AWS credentials
                uses: aws-actions/configure-aws-credentials@v5.1.0
                with:
                    aws-region: us-east-1
                    role-session-name: GitHubActions
                    role-to-assume: arn:aws:iam::831926597587:role/GitHubActionsRole
            -   name: Log in to ECR
                run: |
                    aws ecr get-login-password --region us-east-1 \
                        | helm registry login "${ECR_REGISTRY}" \
                            --password-stdin \
                            --username AWS
            -   name: Checkout code
                uses: actions/checkout@v4
                with:
                    filter: blob:none
                    show-progress: false
            -   env:
                    GITHUB_TOKEN: ${{github.token}}
                id: verify_version
                name: Verify Helm chart version
                run: |
                    set -o errexit

                    CHART_VERSION=$(cat ./Chart.yaml | yq .version)

                    echo "Verifying Helm chart version ${CHART_VERSION}."

                    if ! helm show readme "oci://${ECR_REGISTRY}/liferay/liferay-aws-marketplace:${CHART_VERSION}" >/dev/null 2>&1
                    then
                        echo "Verified Helm Chart version ${CHART_VERSION}."

                        echo "CHART_VERSION=${CHART_VERSION}" >> ${GITHUB_ENV}

                        echo "should_publish=true" >> ${GITHUB_OUTPUT}
                    else
                        echo "Chart version ${CHART_VERSION} already exists."
                    fi
                working-directory: ./cloud/helm/aws-marketplace
            -   if: ${{steps.verify_version.outputs.should_publish == 'true'}}
                name: Package and push Helm chart
                run: |
                    set -o errexit

                    DIST_DIR="${{runner.temp}}/aws-marketplace/dist"

                    rm -fr "${DIST_DIR}"

                    mkdir --parents "${DIST_DIR}"

                    helm dependency update

                    helm package . \
                        --destination "${DIST_DIR}" \
                        --version "${CHART_VERSION}"

                    for file_name in "${DIST_DIR}"/*.tgz
                    do
                        echo "Pushing ${file_name}."

                        helm push "${file_name}" "oci://${ECR_REGISTRY}/liferay"
                    done
                working-directory: ./cloud/helm/aws-marketplace
    publish-default:
        if: |
            (github.event_name == 'push' && github.repository == 'liferay/liferay-portal') ||
            (github.event.action != 'closed' && github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal')
        secrets: inherit
        uses: ./.github/workflows/ci-reusable-publish-helm-chart-to-gar.yaml
        with:
            chart_name: liferay-default
            pr_number: ${{github.event.pull_request.number}}
            registry: us-central1-docker.pkg.dev/liferay-artifact-registry/liferay-helm-chart
            working_directory: ./cloud/helm/default
    publish-gcp:
        if: |
            (github.event_name == 'push' && github.repository == 'liferay/liferay-portal') ||
            (github.event.action != 'closed' && github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal')
        needs: publish-default
        secrets: inherit
        uses: ./.github/workflows/ci-reusable-publish-helm-chart-to-gar.yaml
        with:
            chart_name: liferay-gcp
            pr_number: ${{github.event.pull_request.number}}
            registry: us-central1-docker.pkg.dev/liferay-artifact-registry/liferay-helm-chart
            working_directory: ./cloud/helm/gcp
    publish-gcp-infrastructure:
        if: |
            (github.event_name == 'push' && github.repository == 'liferay/liferay-portal') ||
            (github.event.action != 'closed' && github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal')
        secrets: inherit
        uses: ./.github/workflows/ci-reusable-publish-helm-chart-to-gar.yaml
        with:
            chart_name: liferay-gcp-infrastructure
            pr_number: ${{github.event.pull_request.number}}
            registry: us-central1-docker.pkg.dev/external-assets-prd/liferay-helm-chart
            working_directory: ./cloud/helm/gcp-infrastructure
    publish-gcp-infrastructure-provider:
        if: |
            (github.event_name == 'push' && github.repository == 'liferay/liferay-portal') ||
            (github.event.action != 'closed' && github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal')
        secrets: inherit
        uses: ./.github/workflows/ci-reusable-publish-helm-chart-to-gar.yaml
        with:
            chart_name: liferay-gcp-infrastructure-provider
            pr_number: ${{github.event.pull_request.number}}
            registry: us-central1-docker.pkg.dev/external-assets-prd/liferay-helm-chart
            working_directory: ./cloud/helm/gcp-infrastructure-provider
    release-helm-charts:
        env:
            CR_TOKEN: ${{secrets.GITHUB_TOKEN}}
        if: github.event.pull_request.head.repo.full_name == 'pt-cloudnative/liferay-portal' && github.event.pull_request.number != '' && github.event_name == 'pull_request' && github.repository == 'pt-cloudnative/liferay-portal'
        name: Run chart-releaser
        needs:
            -   publish-aws
            -   publish-aws-infrastructure
            -   publish-aws-infrastructure-provider
            -   publish-default
            -   publish-gcp
            -   publish-gcp-infrastructure
            -   publish-gcp-infrastructure-provider
        permissions:
            contents: write
        runs-on: ubuntu-latest
        steps:
            -   name: Checkout
                uses: actions/checkout@v4
                with:
                    fetch-depth: 1
                    spare-checkout-code-mode: true
                    sparse-checkout: |
                        cloud/helm
            -   name: Configure Git
                run: |
                    git config user.name "$GITHUB_ACTOR"
                    git config user.email "$GITHUB_ACTOR@users.noreply.github.com"
            -   name: Install Helm
                uses: azure/setup-helm@v4
            -   name: Run chart-releaser
                uses: helm/chart-releaser-action@v1.7.0
                with:
                    charts_dir: cloud/helm
                    skip_existing: true
on:
    pull_request:
        branches:
            -   master
        paths:
            -   .github/workflows/ci-publish-cloud-helm-charts.yaml
            -   .github/workflows/ci-reusable-publish-helm-chart-to-gar.yaml
            -   cloud/helm/**
        types:
            -   closed
            -   opened
            -   reopened
            -   synchronize
    push:
        branches:
            -   master
        paths:
            -   .github/workflows/ci-publish-cloud-helm-charts.yaml
            -   .github/workflows/ci-reusable-publish-helm-chart-to-gar.yaml
            -   cloud/helm/**
permissions:
    contents: write
    id-token: write
    packages: write